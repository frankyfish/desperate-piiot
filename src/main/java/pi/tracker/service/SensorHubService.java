package pi.tracker.service;

/**
 * Represents a service which can collect and deliver data from specific sensor board or sensor and deliver it.
 */
public interface SensorHubService {
    void serve();
}
